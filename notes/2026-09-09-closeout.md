# 2026-09-09 — bible-query.py removal (M5 closeout) + Phase 3 Quran scope

## Done

- **Removed `bible-query.py` per ADR-004.** Was the Grok-upgrade CLI,
  deprecated in v0.2.0, fully superseded since v0.3.0 (`bible search`
  BM25 covers keyword search; `bible/` package covers parallel + strongs
  + references). 260 lines deleted, one regression test dropped, all
  references in HANDOFF.md / README.md / CONTRIBUTING.md / SECURITY.md /
  docs/data-schema.md / .github/ISSUE_TEMPLATE/ updated.
- **Suite down from 39 → 38 tests, all passing.** Sister-script runner
  is unchanged in shape.
- **Phase 3 scout for Quran: `docs/phase3-scope-quran.md`.** Decision
  package, not code. Identifies:
  - `fawazahmed0/quran-api` (Unlicense, 1.2k stars, 492 editions) as
    the canonical public-domain source
  - 5-translation shortlist (Saheeh Intl, Yusuf Ali, Pickthall, Mufti
    Taqi Usmani, Arberry — all sourced from tanzil.net)
  - The adapter pattern (`bible/quran.py` mirrors `bible/lookup.py` shape)
  - ~600 lines of estimated work, stdlib throughout, shippable in one
    focused day
  - Risks: Arabic rendering (low — UTF-8 already proven by Hebrew/Greek),
    citation ambiguity (low — "Quran 2:255", "Al-Baqarah 2:255", "2:255"
    all parse), surah-name variants (low — same alias-table pattern as
    Bible books), translation politics (medium — Council call)
- **CHANGELOG v0.5.0**, **HANDOFF §5** (M5 → 99%), **HANDOFF §5 M3B**
  (BM25 marked shipped; semantic-search deferral rationale updated),
  **pyproject.toml** version bump.
- **Milestone 5 is now functionally complete** (closeout minus the
  README refresh that's been pending since v0.1.0 — the README still
  says "partially shipped" in some places; that's a doc nit, not a
  milestone blocker).

## Blocked

- Nothing.

## Tomorrow / Next decisions

- **Phase 3.1 Quran ingestion (~1 day of work, scoped above).** Or:
- **ADR-009 — Milestone 3B embedding model selection.** Or:
- **Polish: README rewrite** (pending since v0.1.0 per HANDOFF §8.3 —
  "the current README frames the project as the Abrahamic/Christian
  slice… a README rewrite is not urgent; do it after Milestone 1").
  Milestone 1 has been done for weeks; this is overdue.

My recommendation: the README rewrite is the smallest, most satisfying
next block (it's been pending longest). The Quran scope doc means
whenever Phase 3 work happens, the next agent knows exactly what to do
without re-doing this research.

## Decisions made

- **ADR-004 (closed out):** `bible-query.py` removed. The legacy CLI
  has zero remaining users; the `bible/` package and `bible search`
  cover every function it had.
- **Phase 3 pilot selection:** Quran chosen as the first non-Christian
  tradition because it's the most schema-analogous to Christian Bible
  and has the cleanest public-domain data path. Documented in
  `docs/phase3-scope-quran.md`. When this ships, becomes ADR-009.

These decisions are recorded in `docs/design-decisions.md` (ADR-004
closeout) and `docs/phase3-scope-quran.md` (Phase 3 plan).

## Verification

```
$ python3 tests/run_all.py
  ... 38 ✓ ...
  38 passed, 0 failed (of 38)

$ python3 -m bible references "John 3:16" --min-votes 50
  (still works after legacy script removal)

$ ls bible-query.py
ls: cannot access 'bible-query.py': No such file or directory
```

## Commit shape

- (Option 2 commit): `feat!: remove bible-query.py (Milestone 5 closeout per ADR-004)`
- (Option 3 commit): `docs: Phase 3 scope — Quran as pilot second tradition`
- (Chore commit): `chore: bump to v0.5.0 — M5 closeout + scope doc`
