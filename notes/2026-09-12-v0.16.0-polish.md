# 2026-09-12 — v0.16.0 polish + front-end scope warm-up

## Done

- **v0.16.0 polish shipped** (commit pending push):
  - **Bug fix:** `bible hybrid -n 5` was rejected with "unrecognized arguments"
    (CLI inconsistency with `bible search -n 5` which has had `-n` since v0.3.0).
    Added `-n` as short alias for `--top-k` in `bible/hybrid.py`.
    README quickstart + `bible/__main__.py` USAGE + module docstring all updated.
  - **Bug fix:** `bible.__version__` was stuck at `0.2.0` since v0.5.0 — 11 minor
    versions of drift from `pyproject.toml`. Bumped to `0.15.0`. Submodule
    docstring now lists all 10 submodules (was 3).
  - **Regression tests:** 3 new sister-script tests
    (`t_hybrid_cli_accepts_dash_n_alias`, `t_hybrid_cli_top_k_still_works`,
    `t_version_matches_pyproject_toml`). Suite: 126 → 129 tests, all passing,
    exit code 0 verified directly (not piped).
  - **CHANGELOG.md:** v0.16.0 entry with Fixed / Tests / Notes sections.

## Blocked
None.

## Tomorrow
- **Front-end scoping (Layer 1 Reader MVP).** The user requested one front-end
  question per persona (Aisha, Marcus, Yuki, Sarah, Jordan, Priya). Each persona
  question surfaces a specific UI requirement; the goal is to write
  `docs/phase4-scope-frontend.md` after all six are answered.
- **Decision:** which question to scope from each persona first? Started with
  Persona 1 (Aisha — academic, smallest audience but highest methodological
  standards; sets the Scholar layer bar). After all six, the MVP scope doc
  should make the Reader-vs-Scholar-vs-Comparative split concrete.

## Decisions made
- **`-n` alias for `--top-k` in `bible hybrid` is ergonomics only.** No semantic
  change. `-n` and `--top-k` are interchangeable; both forms now work in the
  CLI. The regression test guards against accidentally removing the long form.
- **`bible.__version__` follows `pyproject.toml`, not the other way around.**
  The `t_version_matches_pyproject_toml` regression test pins this direction;
  if someone updates `bible/__init__.py` first, the test fails until
  `pyproject.toml` is also bumped. Source of truth = `pyproject.toml [project]
  version`.
- **v0.16.0 is a polish release, not a milestone.** No new capability; just
  CLI consistency + version drift fix. Phase 3.5+ new-tradition work and
  Phase 4 front-end work remain the next milestones.

## Front-end questions being asked (one per persona, in order)

The user requested an outside-influence user-style question for each of the six
personas in `docs/audience_expectations.md`. These will surface the actual
requirements the front-end needs to satisfy — without conflating UI design
choices with the data layer. Questions are being asked one at a time so the
user can shape each response before the next.

Persona order (by stake, per `audience_expectations.md` §0):
1. **Dr. Aisha** — biblical studies professor (academic)
2. **Marcus** — secular wisdom-seeker
3. **Dr. Yuki Tanaka** — non-Abrahamic religious-studies researcher
4. **Sarah** — lay-faithful reader (the load-bearing persona)
5. **Jordan** — AI engineer / RAG builder
6. **Priya** — interfaith dialogue facilitator

The answers will become the input to `docs/phase4-scope-frontend.md` after
all six are collected.